pluginManagement {
    plugins {
        String kotlinVersion = "1.7.0"
        id 'org.jetbrains.kotlin.jvm' version kotlinVersion apply false
    }
}
include ":kotlin-compiler-wrapper"

rootProject.name = "LivePlugin"
